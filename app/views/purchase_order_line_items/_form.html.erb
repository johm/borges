<%= form_for(@purchase_order_line_item) do |f| %>
  <% if @purchase_order_line_item.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@purchase_order_line_item.errors.count, "error") %> prohibited this purchase_order_line_item from being saved:</h2>

      <ul>
      <% @purchase_order_line_item.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :quantity %><br />
    <%= f.number_field :quantity %>
  </div>
  <div class="field">
    <%= f.label :edition %><br />
    <%= f.text_field :edition %>
  </div>
  <div class="field">
    <%= f.label :purchase_order %><br />
    <%= f.text_field :purchase_order %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
